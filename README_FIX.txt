Что исправлено:
1. Убрана зависимость от локального пути I:\blabla\dataset из model/test.py.
2. Исправлен конфликт имён в FastAPI/Celery: endpoint и task теперь не ломают вызов .delay().
3. Передача файла в Celery переведена на base64 + JSON, чтобы не использовать pickle.
4. Загрузка модели сделана безопаснее: сначала ищется h5, потом keras, с compile=False.
5. Зафиксированы версии зависимостей для Docker.
6. Пути к модели переведены на pathlib и абсолютное определение от корня проекта.

Важно:
В этом архиве нет оригинальных файлов модели, потому что из среды редактирования бинарники из GitHub не удалось выгрузить напрямую.
Нужно положить в папку model исходный файл модели из твоего репозитория:
- model/minecraft_biome_model.h5
или
- model/minecraft_biome_model.keras

Запуск:
1. Скопируй файл модели в папку model.
2. Выполни: docker compose up --build
3. API будет доступно на http://localhost:6969
